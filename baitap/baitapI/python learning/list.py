numbers = [5, 2, 1, 7, 4]
numbers.insert(0, 10)
print(numbers)
# we can exchange the type of key words it include
# insert,pop,remove,index(5) this will check the number that exist in the list or not,sort() this will sort the number from zero to ten
# copy()
